using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// 소를 만지면 금동전 표시 UI가 뜬다

public class CowEvent_LYJ : MonoBehaviour
{
    public List<string> transformedObjectNames;
    public float createDistance = 3f;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
